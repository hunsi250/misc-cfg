# venera-src

Personal Venera comic sources.

- `_template_.js` — 源模板 (带注释)
- `_venera_.js` — IDE 补全支持
- `index.json` — 订阅清单 (由服务器分发, 不在本仓库维护)

## 用法

1. 复制 `_template_.js` 为 `your_source.js` 并修改
2. 在 `index.json` 注册 (name/fileName/key/version)
